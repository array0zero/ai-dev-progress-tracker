# PROGRESS.md — 実装進捗記録

project_id: ai-dev-progress-tracker  
source: TASKS.md v2.1 / DESIGN.md v2.1  
v1.3〜v1.7 (T001〜T020) の記録は `PROGRESS-v1.md` を参照。

| タスクID | 状態 | 更新日 | 備考 |
|----------|------|--------|------|
| T001 | 完了 | 2026-09-02 | commit SHA: 3e2eae0。engines を `node>=24.15.0` / `npm>=12.0.2` の下限のみへ変更、`packageManager` と `.nvmrc` を削除、`smol-toml@1.8.0` を完全一致でdependenciesへ追加(lock更新)。CIはnpm/Nodeを下限checkへ変更し `verify:secrets` step追加。smoke testへversion parserの正常/境界case(parse失敗・最小一致・最小-1・上限なし)を追加。検証: `npm ci`→lint→typecheck→test(18 files/165件)→build→e2e(14件)→verify:secrets(hit 0件) 全通過。`git status --porcelain` に検証由来の生成物なし。補足: `src/server/config.ts` の node `maxExclusive` は T004(config.ts担当)で除去する。既存 `tests/unit/version-check.test.ts` はDESIGN v2.1のtreeに未記載だがT001の対象外のため無変更。 |
| T002 | 完了 | 2026-09-02 | commit SHA: 4e262e9。`tests/fixtures/v1-compat/` へ3物理正本(001_init.sql / backup-v1.schema.json / progress-output.schema.json)をbyte-for-byte copyし、byte一致テストと1 byte改変で失敗するnegative testを追加。temp Git repoへ3 fixtureをcommitし`git show HEAD:<path>`のbytes一致を確認。golden 001をfresh temp data dirへ初回loadしv1全テーブル生成を確認。`buildGenerationPrompt`のv1.7意味契約6点(evidence本文のみconfirmed / タイトルのみ・routineはneeds_input / 4field canonical needs_input形 / 常にschema-valid JSON / confirmedはbundle内evidenceId参照 / decision+rationale)を回帰テスト化。`validateProgressOutput`の正規化・decision item除去・UNKNOWN_EVIDENCE_ID・CODEX_OUTPUT_INVALIDは既存testに加えclassification非弱化2件を追加。v1 backup restoreで6テーブル全行一致(論理項目欠損0件)と0件backupのrestore成功を追加。default `recovery-cases.json`に`mustContain`/`mustNotContain`が必須expectedとして存在しないこと、evaluator側では任意checkとして残ることを回帰テスト化。検証: lint/typecheck、test:unit(119件)、test:integration(65件) 通過。 |
| T003 | 完了 | 2026-09-02 | commit SHA: c4864b6。`db/migrations/002_v2.sql` をDESIGN記載どおり追加(projects へ summary/registration_source/review_required/review_required_at、registration_candidates + index)。migration runnerへversion 2を追加し、v1 DBを上げる直前だけ `tracker.db.pre-v2-<UTC>` を1 copy(WAL checkpoint後、自動削除なし)。`RegistrationSource`/`RegistrationCandidateStatus`/`RegistrationCandidate` をdomainへ、`CANDIDATE_NOT_FOUND`/`CANDIDATE_ALREADY_DECIDED` をapi error codeへ追加。project repositoryにv2 field(read必須・write任意でdefault summary=name / manual / review=false)と `setProjectReviewRequired` を追加。candidate repositoryは9 API(upsertDetected/markPrompted/beginRegistration/recordFailure/markRegistered/decline/reopen/list/get)で、state遷移違反はguard付きUPDATEのchanges=0→falseで拒否。検証: test:unit(128件)・test:integration(69件)・全体test(197件)・lint・typecheck・build・e2e(14件) 通過。補足: `LATEST_MIGRATION_VERSION` はrestore側のbackup manifest schemaVersion判定に使われているため値1のまま据え置き(v1/v2分岐はT016)。DB最新versionは `MIGRATIONS` 末尾。既存 db-migrations testの「再applyしない」assertionをversion一覧 [1,2] へ更新。 |
| T004 | 完了 | 2026-09-02 | commit SHA: 4bb1e61。`agent-integration-service.ts`: Codex `~/.codex/config.toml` を smol-toml でparseし、top-level notify不在時のみmanaged block(`# >>> ... >>>`)を最初のtable headerより前へ挿入。別notifyは`CODEX_NOTIFY_CONFLICT`でbyte不変、構文不正は`INVALID_AGENT_CONFIG`で無変更。Claude `~/.claude/settings.json` は未知key・他hookを保持してtracker matcher group 1件をmerge、`disableAllHooks=true`は`CLAUDE_HOOKS_DISABLED`で無変更。`--repair`はtracker entryのみ再生成(path staleを解消)、`--uninstall`はtracker entryのみ除去。`setup-agents` CLIと`npm run setup:agents`を追加。config.tsのversion下限をNode>=24.15.0/Git>=2.45.0/gh>=2.98.0/Codex>=0.152.0/Claude>=2.1.258へ更新し、Nodeの上限(`maxExclusive`)を削除。doctorへClaude Code version + Codex/Claude detection readiness(raw config/token非出力)を追加。system statusへ`agentIntegration`を追加。検証: test:unit(145件)/test全体(214件)/e2e(14件)/lint/typecheck/build 通過。`node dist/cli/index.js doctor` 実行済み。**実機所見(要判断)**: 利用者の`~/.codex/config.toml`に既存のtop-level notify(Codex computer-use `codex-computer-use.exe turn-ended`)があり、doctorが`CODEX_NOTIFY_CONFLICT`を報告してexit 1。AGENTS.mdの停止条件に該当するため、user configは一切変更していない。T009(実機Codex検知)はinvocation-level `--config notify=...`を使うため影響なし。影響するのは`setup-agents`のCodex側とT025 #4の実利用検知。補足: T004対象外だが版数変更の機械的追随として `tests/unit/version-check.test.ts`・`tests/unit/codex-adapter.test.ts`・`tests/helpers/fake-codex.ts` の期待値をv2.1下限へ更新。doctorはsetup前の`not_installed`をWARN(exit codeに非影響)、conflict/stale/disabled/invalidをFAILとした。README(T023/T024担当)のCodex版数記述は未更新。 |
| T005 | 完了 | 2026-09-02 | commit SHA: b5fe16b。`agent-event` CLI: Codexはargv JSON(`type=agent-turn-complete`)、Claudeはstdin JSON(`hook_event_name=UserPromptSubmit`)を解析し、event種別と`cwd`だけを使用。Git配下は`git rev-parse --show-toplevel`、Git外はcwdを`realpathSync.native`でcanonical化(Windowsの実case解決も兼ねる)。既登録projectはno-op、未登録はlocal_path一意でcandidate upsert。status=detectedのときだけserver health確認(未起動なら`dist/server/index.js`をdetached spawn、TRACKER_DATA_DIR/TRACKER_PORTを明示継承、最大3秒/100ms poll)→`/?candidate=<id>`をbrowser open→成功時のみ`prompted`。全経路try/catchでexit 0、合計5秒budgetでgit 3秒・server 3秒・browser 3秒を上限化。`desktop.ts`にopenUrl(win: rundll32 url.dll,FileProtocolHandler / darwin: open / else xdg-open、shell非経由)、spawnDetachedServer、isServerHealthyを追加。検証: `npm run test:integration -- tests/integration/agent-detection.test.ts`(全77件)、全体test 222件、lint/typecheck 通過。テスト内容: Git外初回検知、Claude stdin検知、subdirからtop-level一致(実git再取得値と比較)、同一folder 10 eventで1 candidate・prompt 1回、空/別event/cwd欠落/相対path/不存在dirで0件かつexit 0、既登録projectで0件、browser open失敗時はdetectedのまま次eventで再試行、prompt本文/transcript path/session idがDB・logへ0件。補足: server実spawn経路はT009/T010の実機taskで確認する(unitでは実portを占有しないためinjection)。candidate-repository.tsはT003の実装で足りたため本taskでは無変更。 |
| T006 | 完了 | 2026-09-02 | commit SHA: ef18ebe。`routes/candidates.ts`(GET list/get、POST approve 202・decline・reopen、404 `CANDIDATE_NOT_FOUND`、state違反は409 `CANDIDATE_ALREADY_DECIDED`、400 `INVALID_REQUEST`)、`schemas/candidate.ts`(approve name override <=120 / status filter)。approveはstateをregisteringへ進めるだけで登録本体はT007のworkerへ委譲。`RegistrationPrompt`は`?candidate=<id>`で最優先表示し、registering中だけ500ms pollしてregistered/failedを反映。declineはauto registrationを起動せずcandidate行を保持。`RegistrationCandidatePanel`は未登録candidateをerror code付きで表示し、failed/declinedへ「やり直す」、failedへ「手動で登録」(RegisterProjectFormへname/path prefill、submitまでprojectを作らない)。検証: e2e 18件(新規4件: decline後project 0件・reopen後のGET readback一致・failed→manual prefillでproject未作成・candidate 0件でpanel非表示)、test 222件、lint/typecheck/build 通過。補足: T006対象外の`candidate-repository.ts`へ`beginRegistration`のname override引数を追加(routeにSQLを置かないため)。E2Eのcandidate seedはserverと同じdata dirのSQLiteへ直接upsert(agent-event経路はT005 integration/T009・T010実機で検証)。 |
| T007 | 完了 | 2026-09-02 | commit SHA: dde41e0。`registration-service.ts`にDESIGN §7のstate machine 1〜12を実装(canonical path→Git root判定→未初期化は`git init -b main`→layout/hooksPath制約→originあり=既存repo照合/originなし=`gh auth status`+`gh api user`+固定name正規化+同名repoは`REPOSITORY_NAME_CONFLICT`+`gh repo create --private --source --remote origin`→`gh repo view`でowner/name/PRIVATE再確認→HEAD有無→新規repo かつ HEADありのみ`git push -u origin <branch>`→`git ls-remote`でremote SHA=local HEAD確認→project登録(summary算出/registration_source=agent)→hook設置→HEADありならrecovery enqueue→backup enqueue→candidate=registered)。repo名正規化(NFKC/lower/whitespace→`-`/不正文字→`-`/連続`-`圧縮/前後の`.`-除去/100字truncate/空なら`project-<8hex>`)とsummary算出(description→READMEの最初の非heading段落→project名、240字上限)は純関数として単体テスト化。git adapterへ`describeLocalRepository`(unborn HEADでもbranchを返す`symbolic-ref`使用)・`initRepository`・`setOrigin`・`pushInitial`(60s)・`lsRemoteSha`、github adapterへ`createPrivateRepoFromSource`とrepo viewの`description`を追加。`registration-worker.ts`は`registering` candidateをlast_seen_at昇順で1件ずつ処理(candidate statusが排他)。approve APIが202と同時にworkerをspawn。fake-ghは`repo create --source`で実bare repoを作りoriginを張るため、push/ls-remoteが実Gitで往復する。検証: test:unit 156件/test:integration 82件/全体238件/e2e 18件/lint/typecheck/build 通過。新規統合テスト5件(commit 0件folderの初期化〜登録・push無し確認、commitありのpush後remote SHA=local HEAD、既存originはrepo create未実行、名前衝突で登録0件、push検証不能時は登録0件かつcandidateはregistering維持)。補足: T007対象外の`tests/unit/github-adapter.test.ts`と`tests/unit/backup-export.test.ts`はRepoViewへの`description`追加に伴う機械的更新。 |
| T008 | 完了 | 2026-09-02 | commit SHA: a0193ee。`runRegistrationCycle`を追加: attempt1失敗→redacted error保存→2秒(`REGISTRATION_RETRY_DELAY_MS=2000`固定)→attempt2→失敗で`failed`/attempt_count=2、3回目の自動attemptなし。worker再起動時は`registering`かつ`last_error_code`ありのcandidateをdecision_atから2秒経過を待って残り1回だけ実行(schemaへ列を追加せずdecision_atを基準)。待ちの実体だけ`sleep` seamで差し替え可能にし、間隔値自体は固定。registration workerはcycleを呼ぶよう変更。UIはfailed candidateを消さず`N回試行`を追加表示。検証: `npm run test:integration -- tests/integration/registration-retry.test.ts`(89件)、e2e registration 6件、全体test 245件、lint/typecheck/build 通過。テスト内容: 初回失敗→retryで成功(exactly 2 `gh repo create`、追加操作0)、2回失敗でfailed/attempt_count=2かつworker再実行でも3回目なし、attempt1後にworkerが落ちた状態から残り1回だけ実行、reopen相当のreset後は新しいcycle、retry対象0件でworker正常、成功後のproject readback(実git originがfixture bare repoを指す)。 |
| T009 | 完了 | 2026-09-02 | (chain対応で再実行) commit SHA: 90de614。`scripts/real-check-codex-detection.ts` + `npm run real:codex-detection`。実機結果(PASS): Codex CLI `0.152.1` / ChatGPT認証。OS tempのproject dirとTRACKER_DATA_DIRだけを使い、user `~/.codex/config.toml`はSHA-256一致で無変更を確認。notifyはinvocation-level `-c notify=[...]`で指定。`codex --model gpt-5.6-terra --ask-for-approval never --sandbox read-only -c notify=... exec --skip-git-repo-check` を1 turn実行し、exit 0後にtemp DBへcandidate 1件(agent=codex, status=detected, local_path=実temp cwdのcanonical path一致)を確認。対象外event/空payload/不正JSON/cwd欠落の4件でcandidate数が増えないことも確認。repoの`git status --porcelain`は実行前後で同一。[2026-09-02 chain対応] notify argvへ`--chain`を足して再実行し、chain対象(marker fileを書いてexit 1するscript)が起動されたこと(`chainedNotifyCalled: true`)とcandidate 1件の両立をPASSで確認。補足: T009対象外の`agent-event.ts`へ隔離seam `TRACKER_AGENT_EVENT_PROMPT=off`を追加(実機scriptが利用者の実server/browserを触らずcandidate作成だけを検証するため)。 |
| T010 | 完了 | 2026-09-02 | commit SHA: 54d0f62。`scripts/real-check-claude-detection.ts` + `npm run real:claude-detection`。実機結果(PASS): Claude Code `2.1.258`、`claude auth status` exit 0 / `loggedIn=true`(raw JSONはemail・orgIdを含むため保存も表示もせず、exit codeとloggedInのみ使用)。OS tempのproject/home/TRACKER_DATA_DIRだけを使い、user `~/.claude/settings.json`はSHA-256一致で無変更。temp settingsはinstaller(`applyClaudeHook`)が書く実際の形をそのまま使用し、`claude --restricted -p --settings <temp> --tools ""`を実行→exit 0、temp DBへcandidate 1件(agent=claude, status=detected, local_path=実temp cwd一致)。空stdin/不正JSON/別event/cwd欠落でcandidate数不変。repoの`git status --porcelain`不変。実機所見: DESIGN記載の`--tools ""`は可変長optionのため後続のprompt positionalを飲み込む(`Input must be provided either through stdin or as a prompt argument`でexit 1)。promptをstdinへ渡す形に修正して成功。DESIGNのhook形(command=絶対node実行体 + args配列 + timeout 5)は実Claude Code 2.1.258で機能することを確認。 |
| T011 | 完了 | 2026-09-02 | commit SHA: 5790b37。`scripts/real-check-github-registration.ts` + `npm run real:github-registration`。実機結果(PASS): 対象は `array0zero/ai-dev-progress-tracker-e2e-fixture` のみ。1回目実行でrepoが存在しなかったため自動登録経路(`gh repo create --private --source --remote origin` → 初回push)で新規Private作成し、marker `.tracker-e2e-fixture` を最初のcommitへ含めた(remote SHA=local HEAD一致、`createdRepository: true`)。2回目実行は既存経路: Private + marker確認 → clone → unique fileをcommit/push → `git ls-remote`のremote SHA=local HEAD (`555dcc1`) → fresh cloneのHEADとfile内容がbyte一致 → commit 0件repo(origin=fixture)の登録でremote SHAが不変(push無し)。すべてOS temp、recovery/backupはenqueueしない(Codex・production backupを触らない)。token取得・保存なし。実機所見: fresh cloneはWindowsの`core.autocrlf`でCRLF化しbyte比較が落ちたため、`-c core.autocrlf=false -c core.eol=lf`でcloneして保存bytesをそのまま比較する形に修正(v1のD023と同種の問題)。補足: push/cloneに必要なため`gh auth setup-git`を実行している(v1 backup経路と同じ扱い)。 |
| T012 | 完了 | 2026-09-02 | commit SHA: 5cc6820。`freshness-service.ts`にDESIGNの派生値(`lastGeneratedCommitSha/At`・`unreflected`・`hasNextAction`・`lastUpdatedAt`)とsnapshotなしfallback(HEADなし=`初回コミット待ち` / HEADあり=`進捗生成待ち`)を実装。`readHeads`は最大4並列でlocal HEADを取得、`syncLocalMissing`はlocal path消失時に`local_missing`へ更新して行を保持。未登録HEAD commitはmetadataをupsertして再GETで同値。`lastUpdatedAt`はproject.updated_at / 最新commit.detected_at / snapshot.created_at / review_required_at のmax(backup時刻を含めない)。api.tsへ`ProjectSummaryV2`/`ProjectDetailV2`をv1 field非破壊で追加し、GET /api/projects と GET /api/projects/:id が返すよう配線。検証: `test:unit -- freshness`(164件)、`test:integration -- dashboard-freshness`(96件)、全体260件、lint/typecheck/build/e2e 18件 通過。統合テストは実temp Git repoのHEADをAPIと突き合わせ(0件/HEADなし/snapshot一致/新commitで再度未反映/review時刻反映かつbackup時刻非反映/local_missing)。修正: 初回実装でsnapshotありのneeds_input currentPositionまで固定fallback文字列に置き換えてしまい、v1のE2E(`要補完`表示)が退行した。fallbackはsnapshotが無い場合のみ適用する形へ修正し、v1のfield意味を維持。 |
| T013 | 完了 | 2026-09-02 | commit SHA: 482e941。`DenseProjectRow`(104px固定行 / columns 42-31-12-15% / project名16px 600・現在地14px 500を最上位 / 現在地・次の作業は2行clamp / badge 未反映・要確認・次の作業あり)と`DashboardToolbar`(3 state filterの複数OR、検索UIは`hidden`でT020まで無効、表示件数)を追加。DashboardPageをdense list default・`lastUpdatedAt DESC` / 同値は`name ASC`のsortへ変更。手動登録formは`<details>`で通常時折り畳み(DESIGNの「通常時 collapsed area <=190px」を満たすため)。styles.cssへdense/badge/toolbar/candidate/prompt/manual-registerのlayoutを追加。検証: e2e 22件(新規4件: 2005x1271で8行がscroll無しに収まる+sort順、filter単独/複数OR/解除、project 0件とfilter 0件のempty state、実Git HEADと未反映badge・APIの`latestCommitSha`一致)、test 260件、lint/typecheck/build 通過。補足: 既存e2eのcard DOM前提(`.project-card__sha`・dt見出し)をdense row前提へ書き換え、`registration.spec.ts`(T013対象外)は手動登録formが折り畳みになった追随のみ変更。ProjectCard.tsxは`ProjectSummaryV2`受け取りへ型更新(compact viewで再利用予定)。 |
| T014 | 完了 | 2026-09-02 | commit SHA: debc054。`PATCH /api/projects/:id/review`(true=`review_required_at=now` / false=null、400 INVALID_REQUEST・404 PROJECT_NOT_FOUND)を追加し、`reviewRequestSchema`と`setProjectReviewRequired`で実装。既存の`POST /recover`をdetailの「進捗を再生成」から呼び、trigger=`manual_recovery`。HEADなしのrecoveryは422 `INVALID_REQUEST`(旧`NO_COMMIT_FOR_RECOVERY`から変更、DESIGN §5に合わせた)で開始しない。`ReviewControls`(要確認checkbox + 再生成button)を追加しdetailへ配置。要確認は再生成の成功/失敗で自動解除しない(D012)。検証: `test:integration -- review-regeneration`(100件)、e2e 24件(新規2件: detailでtoggle→HTTP readback→dashboard badge一致→false操作でのみ解除、HEADなしでrecover 422かつrun 0件だがreview設定は可能)、全体test/lint/typecheck/build 通過。実装メモ: controlled checkboxのためreview toggleはoptimistic更新にし、失敗時に元へ戻す(APIラウンドトリップ待ちだと操作が即反映されない)。 |
| T015 | 完了 | 2026-09-02 | commit SHA: ee53a96。`GET /api/projects/:id/history`(newest-first、default 20 / max 100、cursorは`created_at|id`複合で同一時刻でも安定、範囲外limitは400、未知idは404)と`listSnapshotHistory`を追加。`ProgressHistory`componentを現在の状態sectionより後ろへ独立配置(divider + 「進捗履歴」heading + 20件ずつ「さらに読み込む」)。最新snapshotの採用規則(commits.detected_at DESC, created_at DESC)は変更なし。検証: e2e project-detail 11件(新規4件: current/history別section・DOM非ネスト・表示順、履歴0件でも現在の状態を表示し「履歴なし」、21件→20+1のpaginationとAPI cursorのnewest-first一致、limit範囲外400/未知project 404)、全体e2e/test/lint/typecheck/build 通過。 |
| T016 | 完了 | 2026-09-02 | commit SHA: 6159c59。`schemas/backup-v2.schema.json`(v1の6テーブル + project v2 4列 + `registrationCandidates`、additionalProperties禁止)を追加し、`backup.ts`へ`backupDataV2Schema`/`BACKUP_TABLES_V2`/`backupCountsV2Schema`/`backupManifestV2Schema`/`parseBackupManifest`/`backupDataFileName`をv1定義を残したまま追加。production exportはschemaVersion 2 + `data/backup-v2.json`へ変更し、`SELECT *`をやめて列を明示(deterministic、将来列の混入防止)。restoreはmanifestのschemaVersionでv1/v2分岐し、v1 importの後にmigration 002のbackfill(`summary = name`)を再適用。件数検証もversionごとのテーブル集合で行う。`backup-v1.schema.json`と既存v1 zodは無変更。検証: `test:unit -- backup-export`・`test:integration -- backup-v2/backup-flow/restore-flow`(全282件)、lint/typecheck/build/e2e 28件/verify:secrets 通過。新規テスト6件: v2 export内容(summary/source/review_required_at/candidate、backup_runs・worker_leases非出力)、determinism、0件round-trip、v2論理項目の完全一致 + `unreflected`列が存在せずderived再計算で一致、v1 backupのrestoreでv1論理項目欠損0件かつv2列が既定値(summary=name/manual/0)、bare repoへpush→fresh clone→checksum一致→restoreの実Git往復。 |
| T017 | 完了 | 2026-09-02 | commit SHA: c0762e7。`scripts/real-check-backup-restore.ts` + `npm run real:backup-restore`。実機結果(PASS): 対象は `array0zero/ai-dev-progress-tracker-backup-e2e-fixture` のみ(未作成だったのでPrivate作成 + marker `.tracker-e2e-fixture` commit)。production `array0zero/ai-dev-progress-tracker-backup` はslug比較で衝突しないことを確認したうえで参照も更新もしない(`runBackup`の`ensureRepo`/`cloneDir` injectionでfixture slugを渡すだけ、production default名は不変)。v2 field(summary/registration_source/review_required)とregistration_candidate、snapshotをtemp DBへseed→`runBackup`でpush(commit `2f971ec`)→fresh clone(`core.autocrlf=false`)→manifest sha256一致→別temp DBへrestore→project行とcandidate行がsourceと完全一致。0 project backupも同じrepoでpush(commit `98d2537`)→clone→restoreし0件で成功。既存fixtureにmarkerが無くmanifestだけある場合は「fixture所有と確認できない」として無変更で停止する分岐も実装。 |
| T018 | 完了 | 2026-09-02 | commit SHA: 68b7a9a。`scripts/real-check-regeneration.ts` + `npm run real:regeneration`。実機結果(PASS): Codex `0.152.1`。OS tempの実Git repo/DBのみを使い、GitHubへは出ない(fake gh seamでissue/PRを空)。AIだけ実機。richケース(実装差分+設計メモcommit)=run `succeeded` / snapshot `complete`、thinケース(`chore: bump`のみ)=run `unrecoverable` / snapshot残存(`CODEX_OUTPUT_INVALID`でsnapshot 0件にならないv1.7挙動)。両ケースで progress-output schema v1適合、捏造evidence ID 0件、要確認flagは自動解除されず、生成後にlocal HEADを取り直したsnapshot commit SHA一致(未反映解除)。対象repoの`git status --porcelain`不変。自然言語本文は事前expectedにせずschema/status/evidence/SHAだけを機械判定。`npm run eval:recovery`(実機Codex 14 case)= 復元可能10中9 pass(gate>=8)、根拠不足4/4が`unrecoverable`かつ4field needs_input、unknown evidence 0件 → gate PASS。失敗1件はrec-04-worker-leaseの`importantDecisions`がneeds_inputになったモデル揺れ(v1.7基準内)。補足: TASKS.mdのgateコマンドがthin caseを`rec-11`完全一致で選んでいたがfixture idは`rec-11-insufficient-empty`形式のため0件マッチだった。prefix一致(`-like 'rec-11*'`等)へ修正。 |
| T019 | 完了 | 2026-09-02 | commit SHA: 3aa19a9。`CompactProjectCard`(min-height 168px、dense と同じ`ProjectSummaryV2`だけを表示)とtoolbarのview toggle(`dense`/`compact`固定値)を追加。view stateは`localStorage['tracker.dashboard.view.v2']`のみに保存し、切替でAPI mutationを呼ばない。key無し/不正値はdenseへfallback。compactは`min-width:1600px`で3 columns。検証: e2e(新規2件: default dense→toggle→localStorage値→reload復元→invalid値fallback、切替前後で`GET /api/projects`のbodyが完全一致、両viewのempty state)。 |
| T020 | 完了 | 2026-09-02 | commit SHA: 3aa19a9。検索を有効化(T019までhidden)。NFKC→lowercase→trim→whitespace splitでtoken化し、name / summary / owner-repo / 現在地 / 完了事項 / 次の作業 を対象にtoken間AND・field内substringで判定。search と state filter は AND。検証: e2e(新規2件: name/日本語keyword/全角ASCII(NFKC)/2 token AND/空・空白で全件/0件時のsearch-empty表示、APIから取り直した現在地本文で検索して表示と一致、filterとのAND)。全体: test 282件・e2e 31件・lint・typecheck・build 通過。 |
| T021 | 完了 | 2026-09-02 | commit SHA: 75aa534。`scripts/eval-ui-performance.ts` + `npm run eval:ui`。OS tempのTRACKER_DATA_DIRと`TRACKER_PORT=4318`固定(使用中はfailし別portへ逃げない)、実temp Git repo 8件を作りそのHEADでproject/commit/snapshotをseed(現在地・次の作業は実利用に近い長文)。Playwright viewport `2005x1271`。計測はnavigation開始→8行visible=`initialRenderMs`、検索input→結果確定=`searchMs`、filter click→結果確定=`filterMs`。計測前に実Gitの`rev-parse HEAD`とAPIの`latestCommitSha`、DOMのproject名一致を確認(外部往復)。`--dry-run`はseed/server/identity確認まで、`--empty`は0件mode。結果はstdoutへJSON。本taskではobserved fixtureを作らない。対象repoのDB/fileへは書かない。検証: `npm run eval:ui -- --dry-run` 成功、1 run計測と0件modeも成功。実装メモ: Windowsでserverが SQLite を掴んだままだとtemp削除がEPERMになるため、kill後にexitを待ってからretry付きで削除する。 |
| T022 | 完了 | 2026-09-02 | commit SHA: 75aa534。`--record`(= `npm run eval:ui:record`)で5 run + 0件runを実測し、その出力をそのまま`tests/fixtures/ui-performance-observed.json`へ書き込む(手書きなし)。threshold(initial<=2000ms / search<=500ms / filter<=500ms)は事前固定、実測値は固定しない。**実測結果**: initialRenderMs = 171/246/174/165/200、searchMs = 12/11/11/12/12、filterMs = 37/34/35/34/31、0件run = 111/20/44。5/5 runがthreshold以下で`pass: true`。dashboard e2eへfixtureのthreshold回帰(5 run + 0件run + viewport 2005x1271 + threshold値)を追加。検証: e2e 33件・lint 通過。 |
| T023 | 完了 | 2026-09-02 | commit SHA: __T023SHA__。`verify:secrets`の走査対象をDB全ファイル + backup-v2 raw export + `.env.example`へ拡張し、agent event本文(prompt / assistant message / transcript path / session id)のsentinelを追加(16 sentinel / 4 target)。agent-eventをhandler経由で実行してからDB・logを走査し0件を確認。network SaaS SDK(openai / @anthropic-ai / @octokit / aws-sdk / sentry / datadog / segment / posthog / stripe / firebase / supabase等)がdependenciesに無いこともgate化。Codex child envのscrubへ`ANTHROPIC_API_KEY` / `GH_TOKEN` / `GITHUB_TOKEN`を追加(従来はOPENAI_*のみ)。`redaction.test.ts`へ偽token 8種のnegative gate(混入すればscannerが必ず検知し、redaction後は非検知)を追加。`backup-v2.test.ts`へ「commit message中のtoken・worker_leases owner token・backup_runs・auth系keyがbackupへ出ない」回帰を追加。READMEへ使用外部service表(GitHub / 既契約Codex / 既存Claude Code)と追加固定費0円・従量課金0円、SaaS SDK非追加、child env scrub、agent本文非保存を明記。検証: `npm run verify:secrets`(hit 0 / forbiddenDependencies 0)、`test:unit -- redaction`、`test:integration -- backup-v2`、全体test 292件、lint/typecheck 通過。 |
| T024 | 未着手 | 2026-09-02 | 全MUST回帰・README・agent設定最終同期 |
| T025 | 未着手 | 2026-09-02 | 手動確認（最終タスク） |
