# PROGRESS.md — 実装進捗記録

project_id: ai-dev-progress-tracker
source: TASKS.md v1.1 / DESIGN.md v1.1

| タスクID | 状態 | 更新日 | 備考 |
|----------|------|--------|------|
| T001 | 完了 | 2026-09-01 | 検証コマンド全通過。npm 12.0.2はdependency install scriptを既定でblockするため、native依存(better-sqlite3)とesbuildのみpackage.jsonの`allowScripts`で許可した。dependency版数・ファイル構成は変更なし。 |
| T002 | 完了 | 2026-09-01 | SQLite schema/migration/repository基盤。検証(db-migrations+lease-repository+typecheck)通過。better-sqlite3が型を同梱しないため`@types/better-sqlite3@9.6.0`をdevDependenciesへ追加しDESIGN v1.2 D020へ記録(ランタイム依存は不変更)。 |
| T003 | 完了 | 2026-09-01 | process-runner(shell:false/1MiB上限/timeout kill)、redaction(15キー再帰)、logging(JSON Lines/5MiB rotate)、config へ version判定 pure function、doctor CLI、server起動時 Node version gate。検証(redaction+version-check+typecheck+lint)通過。package.json は既存`cli`スクリプトで doctor 起動可能なため変更不要と判断。 [2026-09-01 修正] Windowsで .cmd shim を spawn(shell:false) 起動できない不具合(DESIGN v1.3/D021)。process-runner が PATH+PATHEXT で実体解決し .cmd/.bat は %ComSpec% /d /s /c 経由(shellなし)で起動。gh/git/codex 共通。Windows実行テスト追加。 |
| T004 | 完了 | 2026-09-01 | git adapter(inspectRepository: root realpath/absolute-git-dir/hooksPath/origin正規化/HEAD、NOT_GIT_ROOT・GIT_LAYOUT_UNSUPPORTED・CUSTOM_HOOKS_PATH_UNSUPPORTED・REPOSITORY_MISMATCH)、github adapter(checkAuth/viewRepo/listIssues/listPullRequests、常に-R、body 8000上限、updatedAt降順、Zod検証)。fake-ghは`node <fake-gh.mjs>` seam(TRACKER_GH_BIN/ARGS)でPATH非依存にしWindows/CI両対応。検証(git-adapter+github-adapter+typecheck)通過。 |
| T005 | 完了 | 2026-09-01 | POST /api/projects を固定検証順1..14で実装(15 recovery enqueue/16 backup enqueue は T012/T014へ委譲)。hook-service(shebang保持/非shebangはHOOK_UNSUPPORTEDで無変更/project-id重複ブロックなし)。検証(hook-service+project-registration+typecheck)通過。補足: commitsのCRUD割当ファイルがないため`upsertCommit`等を run-repository.ts へ追加(T008で拡張予定)。buildApp が db 必須になり smoke.test.ts を追随更新。shared/domain.ts は本タスク範囲で変更不要。 |
| T006 | 完了 | 2026-09-01 | GET /api/projects read model、DashboardPage/RegisterProjectForm/ProjectCard(DESIGN表示順)/StatusBanner、App.tsx で `/` 描画。検証(build + dashboard.spec + registration.spec、Chromium 4件)通過。補足: E2Eで実ghを呼ばないため playwright.config.ts を更新(runnerが fake gh + 専用DATA_DIRをenvへ確定、workerはenv参照)。snapshot join は未生成のため read model は null 返し(T011で拡張)。fake-gh.ts へ writeFakeGh/addRepoFixture を追加。 |
| T007 | 完了 | 2026-09-01 | GET /api/projects/:id、snapshot JSONのevidence ID解決(当該project限定→他projectは未解決=SNAPSHOT_INCONSISTENT・500にしない)、decision/rationale/根拠を同一block表示、URLがある時のみ外部link。App.tsx が pathname で `/projects/:id` を分岐。検証(build + project-detail.spec 4件 + typecheck)通過。補足: 整合性エラー用に `SNAPSHOT_INCONSISTENT` を API error 一覧へ追加(DESIGN未記載)。snapshot field 展開は progress-repository.ts の readSnapshotView/resolveEvidence(T009/T010の schemas/progress.ts 前の暫定)。generation/backup status は T011 で配線。styles.css は T007対象外のため未変更。 |
| T008 | 完了 | 2026-09-01 | hook-commit(引数検証→commit upsert→enqueue→spawn、即return)、generation-service(enqueueGeneration: dedupe_key 1件・同一txnでlease取得・stale lease回収でWORKER_LEASE_EXPIRED、startGenerationWorker: 同期spawn失敗でWORKER_SPAWN_FAILED)、generation-worker(lease token検証・detected_at ASC,id ASCで1件ずつrunning遷移・queue空txn内でrelease・取りこぼし防止)、worker/index.ts。run-repository へ claim/hasQueued/markRunTerminal/failRunning 追加。検証(commit-generation-flow 6件 + typecheck)通過。runGeneration は暫定で failed(CODEX_UNAVAILABLE)、T009-T011で本実装。 |
| T009 | 完了 | 2026-09-01 | git adapter に getCommitShow(patch 120,000字上限+truncated)、github は read を1回1秒後retry。generation-service に collectEvidenceBundle(commit/Issue20/PR20、保存前に high-confidence scanner、他repo混入なし、最新snapshotはcontextのみ)。redaction.ts へ redactHighConfidenceSecrets/containsHighConfidenceSecret(GitHub/OpenAI/Anthropic/AWS/PEM/key-value/URL、全量指定子に上限=backtracking回避)。progress-repository へ upsertEvidence/linkRunEvidence/countRunEvidence。検証(evidence-validation + commit-generation-flow 計18件 + typecheck)通過。scanner は T003 の redaction.ts へ追加(T009 file list外だが「秘密情報の扱い」の正規の置き場所、T013/T018 も再利用)。 |
| T010 | 完了 | 2026-09-01 | schemas/progress-output.schema.json (JSON Schema) + src/server/schemas/progress.ts (Zod + validateProgressOutput: needs_input/confirmed 形式、field/item の evidence 1件以上、importantDecisions は field-level のみで items=[] 許可、参照 evidence ID の run_evidence 存在検査、confirmedCount)。adapters/codex.ts: checkCodexReady (version>=0.146.0 → ChatGPT auth の順、raw非表示)、runCodexGeneration (DESIGN固定argv、cwd=OS temp空dir、stdin=prompt、子環境から OPENAI_API_KEY/ORG_ID/PROJECT_ID 除去、exit0+valid JSONのみ)。tests/helpers/fake-codex.ts。generation-service.runGeneration を bundle→checkCodexReady→runCodex→validate まで配線 (snapshot永続化は T011、暫定 failed(SNAPSHOT_PERSIST_PENDING))。検証(progress-schema + codex-adapter 計19件 + typecheck)通過。 [2026-09-01 修正] 実機Codexで判明: (1)version取得不能と下限未満を CODEX_VERSION_CHECK_FAILED / CODEX_AUTH_CHECK_FAILED で区別。(2)progress-output.schema.json が OpenAI structured output 検証で拒否されていた(schemaVersionにtype無し・enumにtype無し・$defs)。全propertyにtype明記しinline化。実機で generation run が partial 成功することを確認。 |
| T011 | 完了 | 2026-09-01 | generation-service: classifyByConfirmedCount(4→succeeded/complete、1..3→partial/partial、0→unrecoverable/unrecoverable) + persistSnapshot(1 transaction で insertSnapshot + markRunTerminal + ai_cli_version 記録)。Codex失敗/timeout/invalid JSON は run=failed で snapshot なし。routes/projects.ts の read model が最新 snapshot(detected_at DESC,created_at DESC)を反映し、古い commit の遅延完了で最終反映 commit を巻き戻さない。generation-worker に handler 未終端時の安全弁。StatusBanner に variant。run-repository に recordCodexCliVersion、progress-repository に getLatestProgress。検証(commit-generation-flow 14件 + build + dashboard.spec 3件)通過。push 非再生成は dedupe で担保。 |
| T012 | 完了 | 2026-09-01 | recovery-service.enqueueRecovery(最新commitから recovery run を queue、active run があれば RUN_ALREADY_ACTIVE、毎回ユニーク dedupe_key)。project-service: 登録後(step15)に snapshot なしなら recovery を自動 enqueue(options.autoRecover/spawnWorker seam)。POST /api/projects/:id/recover(202/404/409)。schemas/progress.ts へ classifyByConfirmedCount(generation-service から移設)・missingFieldKeys・PROGRESS_FIELD_KEYS。ProjectDetailPage に「進捗を再生成」ボタン + 「要補完の項目」一覧(不足field名)。client.ts に recoverProject。検証(recovery-classifier + recovery-flow 計13件 + typecheck)通過。generation-service.enqueueGeneration に dedupeKey override 追加。registerProject シグネチャを (input,db,options) へ変更(callerは全て更新)。 |
| T013 | 完了 | 2026-09-01 | schemas/backup-v1.schema.json + src/server/schemas/backup.ts(6テーブル row/manifest の Zod)。backup-service: exportBackupData(6テーブルをPK昇順・2-space indent・末尾LFで deterministic、backup_runs/worker_leases除外)、createBackupExport(export全文字列へ high-confidence scanner 再適用→検知で SECRET_DETECTED、manifest に SHA-256+counts)、ensureBackupRepo(active login→`<login>/ai-dev-progress-tracker-backup`、既存はPRIVATE必須、無ければ Private 作成、gh auth setup-git)。github adapter へ getActiveLogin/createPrivateRepo/ensureAuthSetupGit。backup-repository へ markBackupRunTerminal/Running/getLatestSuccessfulBackupRun/hasActiveBackupRun。検証(backup-export 6件+typecheck)通過。marker(manifest.appId)検証は local clone を持つ T14 へ。 |
| T014 | 完了 | 2026-09-01 | hook-backup(引数検証→pre_push backup enqueue のみ、generation非呼出、即return)、backup-service.enqueueBackup(global `backup` lease を同一txnで取得)、runBackup(settle待ち: registration/pre_push は対象commitのgeneration terminal最大180s・manualは全active 0件まで/超過は GENERATION_NOT_SETTLED、ensureRepo→clone or pull --ff-only→marker(appId)検証→createBackupExport(secret scan)→差分ありのみcommit/push・差分なしは既存HEAD保存でsucceeded)、backup-worker.processBackupQueue(lease検証・queued_at昇順・queue空txnでrelease)、worker/index.ts は scope=backup で分岐。routes/backup.ts(POST /api/backup 202/409)。project-service step16(autoBackup seam)。検証(backup-flow 7件+typecheck)通過。補足: routes/system.ts を本タスクで新規作成(T017でHost/Origin hardening追加予定、GET /api/system/status)。SystemStatus 型を shared/api.ts へ。backup-repository/run-repository へ claim/has系ヘルパ追加。fake-gh に api user / repo create / auth setup-git を追加。RegisterProjectOptions に autoBackup(全test caller更新)。 |
| T015 | 完了 | 2026-09-01 | restore-service.restoreFromBackup(既存tracker.dbに一切触れず tempDbPath へ構築): manifest(appId/schemaVersion) → data SHA-256 → backup JSON schema → temp SQLite migration → FK OFF で FK-safe 順に6テーブル transaction import → PRAGMA foreign_key_check 0行 → table件数 == manifest counts。いずれか不正なら temp DB(+wal/shm)削除して失敗コード返却。schemas/backup.ts の BACKUP_TABLES へ columns 追加。migrations.ts へ LATEST_MIGRATION_VERSION。検証(restore-flow 5件: 100% import/checksum/FK/件数/既存ファイル不変 + typecheck)通過。 |
| T016 | 完了 | 2026-09-01 | restore-service.performRestore: ensureBackupRepo→backup clone (無ければ clone / あれば pull --ff-only)→ 既存DBあり&&--forceなしは exitCode 2 → restoreFromBackup で検証済み temp DB → --force時は既存DBを `.pre-restore-<ts>` へ rename(sidecarも整理)→ atomic renameSync で tracker.db 置換 → 各project: local_path が存在し inspectRepository ok なら installHooks 再設置、それ以外は status=local_missing(行は保持)。cli/commands/restore.ts + cli/index.ts に `restore` / `restore --force`。hook-service に areHooksInstalled。検証(restore-flow 9件: performRestore 4件含む + build)通過。補足: github.ts は本タスクで追加変更不要(getActiveLogin は T13、clone/pull は runProcess直呼び)。 |
| T017 | 完了 | 2026-09-01 | app.ts に onRequest hook: Host hostname が {127.0.0.1, localhost, ::1} 以外は 403 FORBIDDEN_HOST(ポートは強制しない=DNS rebinding 対策の本質は hostname、かつ app.inject 互換)、mutation method(POST/PUT/PATCH/DELETE)で外部 Origin は 403 FORBIDDEN_ORIGIN、CORS header 不付与。shared/api.ts へ FORBIDDEN_HOST/FORBIDDEN_ORIGIN。index.ts の app.listen 前へ「host は 127.0.0.1 固定・env 変更不可」コメント。DashboardPage は /api/system/status を取得し latestGenerationFailure/latestBackupFailure を上部 warning banner 表示。StatusBanner に label prop 追加、role は error のみ alert・warning は status(既存の getByRole('alert') 前提テストと共存)。検証(build + dashboard E2E 6件 + typecheck)通過。全回帰: unit/integration 146件・E2E 12件通過。補足: routes/system.ts は T014 で新規作成済のため本タスクでは変更なし(Host/Origin は app.ts 側)。system-status fetch は client.ts に足さず DashboardPage 内へインライン。registration.spec.ts の getByRole('alert') 競合は StatusBanner の role 分岐で解消(テストファイル無変更)。 |
| T018 | 完了 | 2026-09-01 | 新規: tests/fixtures/generation-cases.json(10件)、tests/fixtures/recovery-cases.json(復元可能10+根拠不足4=14件)、scripts/eval-generation.ts(--repo <abs-git-root> で file変更→commit→run terminal を fixture 分反復、DESIGN 5規則 + started_at-detected_at<=60s を判定、JSONで pass/fail件数と latency 出力、CI非実行)、scripts/eval-recovery.ts(fixture evidence から prompt を組み Codex 出力を 5規則 + expectedRecoveryStatus で判定、CI非実行)、scripts/verify-no-secrets.ts(GitHub/OpenAI/Anthropic/AWS/PEM/password/token sentinel を env と evidence 到達コンテンツへ置き、fake gh/codex で project登録→generation→backup export→logging を実行、data dir 全ファイル + backup export raw を走査、SENTINEL 生存 0件で exit 0)。変更: commit-generation-flow.test.ts に「10連続 commit run の started_at-detected_at<=5s」「silent failure 3種 (exit1 / invalid JSON / unknown evidence ID) が全件 failed で snapshot なし」「test data dir で server start/stop 10回 10/10」を追加。recovery-flow.test.ts に recovery fixture 整合性テスト (14件・4 unrecoverable・requiredEvidenceExternalKeys が各ケース evidence に存在) を追加。dashboard.spec.ts に「100 project × 20 snapshot を 2秒以内」、project-detail.spec.ts に「evidence 100件を 2秒以内」を追加。検証: `npm run test:all`(lint/typecheck/test 150件/build/e2e 14件) 通過、`npm run verify:secrets` 0件通過。補足/申し送り: (1) 登録 step14 と hook-commit は `commits.message` を redaction 未通過のまま DB 保存する。DESIGN「commit message を DB 保存前に scanner へ通す」に対する不足。本タスク(試験/harness)の範囲外かつ T018 point6 の sentinel 配置先は env と evidence 到達コンテンツのため未修正。人手で production 側の是正要否を判断されたい。(2) `redactHighConfidenceSecrets` は `key=[REDACTED]` の直後に非空白 (JSON エスケープ改行 `\n` 等) が続くと KEY_VALUE_PATTERN が再マッチし非冪等。このため `password=`/`token=` を含む evidence 本文があると `createBackupExport` の secret gate が誤 SECRET_DETECTED になり得る。verify-no-secrets は「push される実バイト列 (raw export) を走査し sentinel 生存 0件」を要件とし、gate 合否は情報として別報告する形で回避。人手確認を推奨。(3) scripts/ は tsconfig.json の include 外のため `npm run typecheck` 非対象 (tsx 実行・biome lint clean)。DESIGN のディレクトリ構成でも scripts 配下は当該3ファイルのみ記載のため eval の共有ヘルパは各 script へインライン。 |
| T019 | 完了 | 2026-09-01 | 新規 README.md: 必要バージョン表 (Node `>=24.15.0 <25` / npm `12.0.2` / Git `>=2.45.0` / gh `>=2.98.0` / Codex `>=0.146.0`)、依存は範囲指定子なし完全一致固定で dependencies 6件・devDependencies 11件を明記、`gh auth login`/`codex login`/`codex login status`/`npm run cli -- doctor`/`npm run build`/`npm start`、プロジェクト登録手順 (dashboard フォーム・origin 一致・worktree/hooksPath 非対応)、commit 後の非同期生成と push 時 backup、`restore` / `restore --force` と backup/restore 動作確認手順、API key を標準経路に使わない旨 (ChatGPT 認証のみ・OPENAI_* を子プロセスへ渡さない・gh auth token を呼ばない・token/password を DB/log/backup へ保存しない)、開発コマンド表、CI 手順を記載。変更 .github/workflows/ci.yml: Node バージョン範囲チェックを `>=24.15.0` かつ `<25` の両境界を明示する式へ整理 (それ以外の 13 手順は既に DESIGN §8 と一致)。検証: `cmp AGENTS.md CLAUDE.md` 一致、`npm run test:all` (test 150件 / e2e 14件) 通過、`npm run verify:secrets` 0件通過。 |
| T020 | 進行中 | 2026-09-01 | 自動実施分は完了。検証コマンド `npm run lint && npm run typecheck && npm test && npm run build && npm run test:e2e && npm run verify:secrets` 全通過 (lint/typecheck clean、test 154件、e2e 14件、verify:secrets 0件)。項目別: #1 doctor=PASS (Node 24.15.0 / Git 2.45.1 / gh 2.98.0 / Codex 0.152.0 / gh auth OK / Codex ChatGPT auth OK)。#15 verify:secrets=PASS (0件)。#3・#6・#14 の 2秒/1分/3分 表示要件は E2E の性能テスト (dashboard 100project<2s、detail 100evidence<2s、100project×20snapshot<2s) で機械計測済み・PASS だが、実データでの人手タイミング確認は未実施。#7 eval:generation は実行し **10/10 pass**（後述の是正後、`--repo D:/ai-dev-progress-tracker`、start latency も 10/10 60秒以内）。#10・#11 eval:recovery は実 Codex を消費するため未実行 (harness は実装・lint 通過済み)。#2・#4・#5・#8・#9・#12・#13・#16 は実 GitHub テスト用 repo A/B・ブラウザ操作・GitHub UI 確認・請求確認を伴い、人手でのみ実施可能。→ 受け入れ完了には利用者による #2〜#14・#16 の実施と、generation ≥8/10・recovery ≥8/10・backup 100%復元・新規固定費/従量請求 0円 の確認が必要。申し送り: 過去セッションの restore CLI スモークテストで実 GitHub 上に private 空 repo `array0zero/ai-dev-progress-tracker-backup` が作成済み (DESIGN 想定の backup 先。#12 の確認対象)。旧申し送り2 (commit message が redaction 未通過で DB 保存) と旧申し送り3 (secret scanner の非冪等性) は後続コミットで修正済み: `upsertCommit` が保存前に `redactHighConfidenceSecrets` を通す (登録・hook-commit の両経路が集約)、`redactHighConfidenceSecrets` を冪等化 (`[REDACTED]` 直後に非空白が続いても再マッチしない)。回帰テスト 4件追加。実機で判明した空 backup repo (`gh repo create` 直後で commit 0・`branches` が `[]`) の初回同期不具合 (`BACKUP_PULL_FAILED`) を修正: commit のある clone のみ `pull --ff-only` し、unborn HEAD では pull をスキップ、push は `git push -u origin main` + commit 前に `symbolic-ref HEAD refs/heads/main`。DESIGN v1.4 へ反映 (§7「バックアップ repo の取得・commit・push」、設計判断ログ D022)。backup-flow 回帰テスト 2件追加 (計 test 156件)。実機で判明した Windows `core.autocrlf` による backup checksum 不一致 (`BACKUP_CHECKSUM_MISMATCH`「data SHA-256 does not match the manifest」) を修正: backup repo root に `.gitattributes` (`* -text`) を追加し `manifest.json` / `data/backup-v1.json` を改行変換対象から外す、既存 repo は次回 backup で `.gitattributes` を追加 commit して置換、clone 再利用時は `git pull` 後 `git checkout HEAD -- .` で再展開、backup commit は `-c commit.gpgsign=false`。DESIGN v1.5 へ反映 (§7 更新、設計判断ログ D023)。回帰テスト 3件追加 (backup-flow 1件 + restore-flow に実 git 往復テスト: bare repo へ実 `runBackup` push → `core.autocrlf=true` で clone → 読み直したデータで `restoreFromBackup` の checksum 一致を確認) (計 test 158件)。**重大**: 初回 eval:generation 実行時、旧 `eval-generation.ts` が `--repo` に直接 commit する設計だったため、対象を本 repo にした結果 fixture の stub 内容で実ソース10ファイルを上書きする junk commit 10件が積まれた → `git reset --hard da46a51` で復旧済み。修正: eval-generation.ts は HEAD から切り出した detached `git worktree` 内でのみ commit し終了時に破棄、fixture の `files` は `docs/eval/<id>.md` 新規のみに変更。さらに fixture の期待値を実測へ是正: `importantDecisions` は confirmed + 根拠（旧: 誤って needs_input）、`nextActions` は単一 commit では根拠不足のため needs_input、`mustContain`/`mustNotContain` の substring 一致は必須条件から除外（confirmed field は根拠必須 + status 一致 + latency ≤60s で判定）。再実行で 10/10 pass。DESIGN v1.6 へ反映（§8「必須評価fixture」更新、設計判断ログ D024）。 |
